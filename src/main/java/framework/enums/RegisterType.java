package framework.enums;

public enum RegisterType {
    VALID_USER,
    INVALID_NAME,
    INVALID_EMAIL,
    INVALID_PASSWORD,
    INVALID_PHONE,
    INVALID_BIRTHDAY,
    INVALID_GENDER,
    EMPTY_USER
}
